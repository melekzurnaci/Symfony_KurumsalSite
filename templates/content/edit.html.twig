{#{% extends 'base.html.twig' %}#}

{#{% block title %}Edit Content{% endblock %}#}

{#{% block body %}#}
{#    <h1>Edit Content</h1>#}

{#    {{ include('user/content/_form.html.twig', {'button_label': 'Update'}) }}#}

{#    <a href="{{ path('user_content_index') }}">back to list</a>#}

{#    {{ include('user/content/_delete_form.html.twig') }}#}
{#{% endblock %}#}
{% extends 'homebase.html.twig' %}

{% block title %}Edit content{% endblock %}

{% block body %}    <br><br><br><br><br><br><br><br><br><br>
    {{ include ('user/usermenu.html.twig') }}
    <div class="container">
        <div>
            <div class="col">
                <div class="section_title_container text-center">
                    <div class="section_subtitle"></div>
                    <div class="section_title">My Announcement</div>
                </div>
            </div>
        </div>

        <div class="row news_row">
            <div class="news_item">
                <div class="news_content">
                    <div class="news_title" style="text-align: center">New Regulations on the Crypto Market</div>
                    <div class="news_text">
                        {{ include('content/_form.html.twig', {'button_label': 'Update'}) }}
                        <br>
                        <div class="col-md-2">
                            <a href="{{ path('user_content_index') }}" class="btn btn-primary">Back to list</a>
                        </div>
                        <br>
                        {{ include('content/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
